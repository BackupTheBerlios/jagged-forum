You can use the 'setup.sh' script to create a compatible MySQL database. Be sure to modify the 'setup.sh' script with the appropriate username and password.

This is just a temporary "installation" system until a better system is written.
